// Now replace by the generic getById.ts
// import { sorts } from "../data/sorts";

// const getSortingById = (id?: string) => {
//   return sorts.find((sort) => sort.id === id);
// };

// export default getSortingById;
